version: 2

models:
  - name: stg_online_retail
    description: "Staging model for the online retail data"
    columns:
      - name: InvoiceNo
        data_tests:
          - not_null
          - unique
      - name: CustomerID
        data_tests:
          - not_null

sources:
  - name: public
    schema: public
    tables:
      - name: online_retail
        columns:
          - name: InvoiceNo
            data_tests:
              - not_null
              - unique
